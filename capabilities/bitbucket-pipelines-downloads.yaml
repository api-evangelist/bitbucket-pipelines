naftiko: 1.0.0-alpha2
info:
  label: Bitbucket API — Downloads
  description: 'Bitbucket API — Downloads. 4 operations. Lead operation: List download artifacts. Self-contained Naftiko capability
    covering one Bitbucket Pipelines business surface.'
  tags:
  - Bitbucket Pipelines
  - Downloads
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    BITBUCKET_PIPELINES_API_KEY: BITBUCKET_PIPELINES_API_KEY
capability:
  consumes:
  - type: http
    namespace: bitbucket-pipelines-downloads
    baseUri: https://api.bitbucket.org/2.0
    description: Bitbucket API — Downloads business capability. Self-contained, no shared references.
    resources:
    - name: repositories-workspace-repo_slug-downloads
      path: /repositories/{workspace}/{repo_slug}/downloads
      operations:
      - name: get
        method: GET
        description: List download artifacts
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: post
        method: POST
        description: Upload a download artifact
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    - name: repositories-workspace-repo_slug-downloads-filename
      path: /repositories/{workspace}/{repo_slug}/downloads/{filename}
      operations:
      - name: delete
        method: DELETE
        description: Delete a download artifact
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: get
        method: GET
        description: Get a download artifact link
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: bitbucket-pipelines-downloads-rest
    port: 8080
    description: REST adapter for Bitbucket API — Downloads. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/repositories/{workspace}/{repo-slug}/downloads
      name: repositories-workspace-repo-slug-downloads
      description: REST surface for repositories-workspace-repo_slug-downloads.
      operations:
      - method: GET
        name: get
        description: List download artifacts
        call: bitbucket-pipelines-downloads.get
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: post
        description: Upload a download artifact
        call: bitbucket-pipelines-downloads.post
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/repositories/{workspace}/{repo-slug}/downloads/{filename}
      name: repositories-workspace-repo-slug-downloads-filename
      description: REST surface for repositories-workspace-repo_slug-downloads-filename.
      operations:
      - method: DELETE
        name: delete
        description: Delete a download artifact
        call: bitbucket-pipelines-downloads.delete
        outputParameters:
        - type: object
          mapping: $.
      - method: GET
        name: get
        description: Get a download artifact link
        call: bitbucket-pipelines-downloads.get
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: bitbucket-pipelines-downloads-mcp
    port: 9090
    transport: http
    description: MCP adapter for Bitbucket API — Downloads. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: list-download-artifacts
      description: List download artifacts
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: bitbucket-pipelines-downloads.get
      outputParameters:
      - type: object
        mapping: $.
    - name: upload-download-artifact
      description: Upload a download artifact
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: bitbucket-pipelines-downloads.post
      outputParameters:
      - type: object
        mapping: $.
    - name: delete-download-artifact
      description: Delete a download artifact
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: bitbucket-pipelines-downloads.delete
      outputParameters:
      - type: object
        mapping: $.
    - name: get-download-artifact-link
      description: Get a download artifact link
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: bitbucket-pipelines-downloads.get
      outputParameters:
      - type: object
        mapping: $.
