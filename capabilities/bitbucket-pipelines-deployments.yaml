naftiko: 1.0.0-alpha2
info:
  label: Bitbucket API — Deployments
  description: 'Bitbucket API — Deployments. 16 operations. Lead operation: List repository deploy keys. Self-contained Naftiko
    capability covering one Bitbucket Pipelines business surface.'
  tags:
  - Bitbucket Pipelines
  - Deployments
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    BITBUCKET_PIPELINES_API_KEY: BITBUCKET_PIPELINES_API_KEY
capability:
  consumes:
  - type: http
    namespace: bitbucket-pipelines-deployments
    baseUri: https://api.bitbucket.org/2.0
    description: Bitbucket API — Deployments business capability. Self-contained, no shared references.
    resources:
    - name: repositories-workspace-repo_slug-deploy-keys
      path: /repositories/{workspace}/{repo_slug}/deploy-keys
      operations:
      - name: get
        method: GET
        description: List repository deploy keys
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: post
        method: POST
        description: Add a repository deploy key
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    - name: repositories-workspace-repo_slug-deploy-keys-key_id
      path: /repositories/{workspace}/{repo_slug}/deploy-keys/{key_id}
      operations:
      - name: delete
        method: DELETE
        description: Delete a repository deploy key
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: get
        method: GET
        description: Get a repository deploy key
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: put
        method: PUT
        description: Update a repository deploy key
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    - name: repositories-workspace-repo_slug-deployments
      path: /repositories/{workspace}/{repo_slug}/deployments
      operations:
      - name: getdeploymentsforrepository
        method: GET
        description: List deployments
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: workspace
          in: path
          type: string
          description: This can either be the workspace ID (slug) or the workspace UUID surrounded by curly-braces, for example
            `{workspace UUID}`.
          required: true
        - name: repo_slug
          in: path
          type: string
          description: The repository.
          required: true
    - name: repositories-workspace-repo_slug-deployments-deployment_uuid
      path: /repositories/{workspace}/{repo_slug}/deployments/{deployment_uuid}
      operations:
      - name: getdeploymentforrepository
        method: GET
        description: Get a deployment
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: workspace
          in: path
          type: string
          description: This can either be the workspace ID (slug) or the workspace UUID surrounded by curly-braces, for example
            `{workspace UUID}`.
          required: true
        - name: repo_slug
          in: path
          type: string
          description: The repository.
          required: true
        - name: deployment_uuid
          in: path
          type: string
          description: The deployment UUID.
          required: true
    - name: repositories-workspace-repo_slug-environments
      path: /repositories/{workspace}/{repo_slug}/environments
      operations:
      - name: getenvironmentsforrepository
        method: GET
        description: List environments
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: workspace
          in: path
          type: string
          description: This can either be the workspace ID (slug) or the workspace UUID surrounded by curly-braces, for example
            `{workspace UUID}`.
          required: true
        - name: repo_slug
          in: path
          type: string
          description: The repository.
          required: true
      - name: createenvironment
        method: POST
        description: Create an environment
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: workspace
          in: path
          type: string
          description: This can either be the workspace ID (slug) or the workspace UUID surrounded by curly-braces, for example
            `{workspace UUID}`.
          required: true
        - name: repo_slug
          in: path
          type: string
          description: The repository.
          required: true
        - name: _body
          in: body
          type: string
          description: The environment to create.
          required: true
    - name: repositories-workspace-repo_slug-environments-environment_uuid
      path: /repositories/{workspace}/{repo_slug}/environments/{environment_uuid}
      operations:
      - name: getenvironmentforrepository
        method: GET
        description: Get an environment
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: workspace
          in: path
          type: string
          description: This can either be the workspace ID (slug) or the workspace UUID surrounded by curly-braces, for example
            `{workspace UUID}`.
          required: true
        - name: repo_slug
          in: path
          type: string
          description: The repository.
          required: true
        - name: environment_uuid
          in: path
          type: string
          description: The environment UUID.
          required: true
      - name: deleteenvironmentforrepository
        method: DELETE
        description: Delete an environment
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: workspace
          in: path
          type: string
          description: This can either be the workspace ID (slug) or the workspace UUID surrounded by curly-braces, for example
            `{workspace UUID}`.
          required: true
        - name: repo_slug
          in: path
          type: string
          description: The repository.
          required: true
        - name: environment_uuid
          in: path
          type: string
          description: The environment UUID.
          required: true
    - name: repositories-workspace-repo_slug-environments-environment_uuid-changes
      path: /repositories/{workspace}/{repo_slug}/environments/{environment_uuid}/changes
      operations:
      - name: updateenvironmentforrepository
        method: POST
        description: Update an environment
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: workspace
          in: path
          type: string
          description: This can either be the workspace ID (slug) or the workspace UUID surrounded by curly-braces, for example
            `{workspace UUID}`.
          required: true
        - name: repo_slug
          in: path
          type: string
          description: The repository.
          required: true
        - name: environment_uuid
          in: path
          type: string
          description: The environment UUID.
          required: true
    - name: workspaces-workspace-projects-project_key-deploy-keys
      path: /workspaces/{workspace}/projects/{project_key}/deploy-keys
      operations:
      - name: get
        method: GET
        description: List project deploy keys
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: post
        method: POST
        description: Create a project deploy key
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    - name: workspaces-workspace-projects-project_key-deploy-keys-key_id
      path: /workspaces/{workspace}/projects/{project_key}/deploy-keys/{key_id}
      operations:
      - name: delete
        method: DELETE
        description: Delete a deploy key from a project
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: get
        method: GET
        description: Get a project deploy key
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: bitbucket-pipelines-deployments-rest
    port: 8080
    description: REST adapter for Bitbucket API — Deployments. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/repositories/{workspace}/{repo-slug}/deploy-keys
      name: repositories-workspace-repo-slug-deploy-keys
      description: REST surface for repositories-workspace-repo_slug-deploy-keys.
      operations:
      - method: GET
        name: get
        description: List repository deploy keys
        call: bitbucket-pipelines-deployments.get
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: post
        description: Add a repository deploy key
        call: bitbucket-pipelines-deployments.post
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/repositories/{workspace}/{repo-slug}/deploy-keys/{key-id}
      name: repositories-workspace-repo-slug-deploy-keys-key-id
      description: REST surface for repositories-workspace-repo_slug-deploy-keys-key_id.
      operations:
      - method: DELETE
        name: delete
        description: Delete a repository deploy key
        call: bitbucket-pipelines-deployments.delete
        outputParameters:
        - type: object
          mapping: $.
      - method: GET
        name: get
        description: Get a repository deploy key
        call: bitbucket-pipelines-deployments.get
        outputParameters:
        - type: object
          mapping: $.
      - method: PUT
        name: put
        description: Update a repository deploy key
        call: bitbucket-pipelines-deployments.put
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/repositories/{workspace}/{repo-slug}/deployments
      name: repositories-workspace-repo-slug-deployments
      description: REST surface for repositories-workspace-repo_slug-deployments.
      operations:
      - method: GET
        name: getdeploymentsforrepository
        description: List deployments
        call: bitbucket-pipelines-deployments.getdeploymentsforrepository
        with:
          workspace: rest.workspace
          repo_slug: rest.repo_slug
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/repositories/{workspace}/{repo-slug}/deployments/{deployment-uuid}
      name: repositories-workspace-repo-slug-deployments-deployment-uuid
      description: REST surface for repositories-workspace-repo_slug-deployments-deployment_uuid.
      operations:
      - method: GET
        name: getdeploymentforrepository
        description: Get a deployment
        call: bitbucket-pipelines-deployments.getdeploymentforrepository
        with:
          workspace: rest.workspace
          repo_slug: rest.repo_slug
          deployment_uuid: rest.deployment_uuid
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/repositories/{workspace}/{repo-slug}/environments
      name: repositories-workspace-repo-slug-environments
      description: REST surface for repositories-workspace-repo_slug-environments.
      operations:
      - method: GET
        name: getenvironmentsforrepository
        description: List environments
        call: bitbucket-pipelines-deployments.getenvironmentsforrepository
        with:
          workspace: rest.workspace
          repo_slug: rest.repo_slug
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: createenvironment
        description: Create an environment
        call: bitbucket-pipelines-deployments.createenvironment
        with:
          workspace: rest.workspace
          repo_slug: rest.repo_slug
          _body: rest._body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/repositories/{workspace}/{repo-slug}/environments/{environment-uuid}
      name: repositories-workspace-repo-slug-environments-environment-uuid
      description: REST surface for repositories-workspace-repo_slug-environments-environment_uuid.
      operations:
      - method: GET
        name: getenvironmentforrepository
        description: Get an environment
        call: bitbucket-pipelines-deployments.getenvironmentforrepository
        with:
          workspace: rest.workspace
          repo_slug: rest.repo_slug
          environment_uuid: rest.environment_uuid
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: deleteenvironmentforrepository
        description: Delete an environment
        call: bitbucket-pipelines-deployments.deleteenvironmentforrepository
        with:
          workspace: rest.workspace
          repo_slug: rest.repo_slug
          environment_uuid: rest.environment_uuid
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/repositories/{workspace}/{repo-slug}/environments/{environment-uuid}/changes
      name: repositories-workspace-repo-slug-environments-environment-uuid-changes
      description: REST surface for repositories-workspace-repo_slug-environments-environment_uuid-changes.
      operations:
      - method: POST
        name: updateenvironmentforrepository
        description: Update an environment
        call: bitbucket-pipelines-deployments.updateenvironmentforrepository
        with:
          workspace: rest.workspace
          repo_slug: rest.repo_slug
          environment_uuid: rest.environment_uuid
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/workspaces/{workspace}/projects/{project-key}/deploy-keys
      name: workspaces-workspace-projects-project-key-deploy-keys
      description: REST surface for workspaces-workspace-projects-project_key-deploy-keys.
      operations:
      - method: GET
        name: get
        description: List project deploy keys
        call: bitbucket-pipelines-deployments.get
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: post
        description: Create a project deploy key
        call: bitbucket-pipelines-deployments.post
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/workspaces/{workspace}/projects/{project-key}/deploy-keys/{key-id}
      name: workspaces-workspace-projects-project-key-deploy-keys-key-id
      description: REST surface for workspaces-workspace-projects-project_key-deploy-keys-key_id.
      operations:
      - method: DELETE
        name: delete
        description: Delete a deploy key from a project
        call: bitbucket-pipelines-deployments.delete
        outputParameters:
        - type: object
          mapping: $.
      - method: GET
        name: get
        description: Get a project deploy key
        call: bitbucket-pipelines-deployments.get
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: bitbucket-pipelines-deployments-mcp
    port: 9090
    transport: http
    description: MCP adapter for Bitbucket API — Deployments. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: list-repository-deploy-keys
      description: List repository deploy keys
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: bitbucket-pipelines-deployments.get
      outputParameters:
      - type: object
        mapping: $.
    - name: add-repository-deploy-key
      description: Add a repository deploy key
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: bitbucket-pipelines-deployments.post
      outputParameters:
      - type: object
        mapping: $.
    - name: delete-repository-deploy-key
      description: Delete a repository deploy key
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: bitbucket-pipelines-deployments.delete
      outputParameters:
      - type: object
        mapping: $.
    - name: get-repository-deploy-key
      description: Get a repository deploy key
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: bitbucket-pipelines-deployments.get
      outputParameters:
      - type: object
        mapping: $.
    - name: update-repository-deploy-key
      description: Update a repository deploy key
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: bitbucket-pipelines-deployments.put
      outputParameters:
      - type: object
        mapping: $.
    - name: list-deployments
      description: List deployments
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: bitbucket-pipelines-deployments.getdeploymentsforrepository
      with:
        workspace: tools.workspace
        repo_slug: tools.repo_slug
      outputParameters:
      - type: object
        mapping: $.
    - name: get-deployment
      description: Get a deployment
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: bitbucket-pipelines-deployments.getdeploymentforrepository
      with:
        workspace: tools.workspace
        repo_slug: tools.repo_slug
        deployment_uuid: tools.deployment_uuid
      outputParameters:
      - type: object
        mapping: $.
    - name: list-environments
      description: List environments
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: bitbucket-pipelines-deployments.getenvironmentsforrepository
      with:
        workspace: tools.workspace
        repo_slug: tools.repo_slug
      outputParameters:
      - type: object
        mapping: $.
    - name: create-environment
      description: Create an environment
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: bitbucket-pipelines-deployments.createenvironment
      with:
        workspace: tools.workspace
        repo_slug: tools.repo_slug
        _body: tools._body
      outputParameters:
      - type: object
        mapping: $.
    - name: get-environment
      description: Get an environment
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: bitbucket-pipelines-deployments.getenvironmentforrepository
      with:
        workspace: tools.workspace
        repo_slug: tools.repo_slug
        environment_uuid: tools.environment_uuid
      outputParameters:
      - type: object
        mapping: $.
    - name: delete-environment
      description: Delete an environment
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: bitbucket-pipelines-deployments.deleteenvironmentforrepository
      with:
        workspace: tools.workspace
        repo_slug: tools.repo_slug
        environment_uuid: tools.environment_uuid
      outputParameters:
      - type: object
        mapping: $.
    - name: update-environment
      description: Update an environment
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: bitbucket-pipelines-deployments.updateenvironmentforrepository
      with:
        workspace: tools.workspace
        repo_slug: tools.repo_slug
        environment_uuid: tools.environment_uuid
      outputParameters:
      - type: object
        mapping: $.
    - name: list-project-deploy-keys
      description: List project deploy keys
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: bitbucket-pipelines-deployments.get
      outputParameters:
      - type: object
        mapping: $.
    - name: create-project-deploy-key
      description: Create a project deploy key
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: bitbucket-pipelines-deployments.post
      outputParameters:
      - type: object
        mapping: $.
    - name: delete-deploy-key-project
      description: Delete a deploy key from a project
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: bitbucket-pipelines-deployments.delete
      outputParameters:
      - type: object
        mapping: $.
    - name: get-project-deploy-key
      description: Get a project deploy key
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: bitbucket-pipelines-deployments.get
      outputParameters:
      - type: object
        mapping: $.
